import DivHeader from '../DivHeader';
import './Style.css';

export default () => {
    return(
        <main>
            <div className={"arredondada"}>
                <DivHeader>Alguma coisa</DivHeader>
            </div>
        </main>
    );
}